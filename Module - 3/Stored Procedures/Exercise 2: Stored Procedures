CREATE TABLE Accounts (
    AccountID NUMBER PRIMARY KEY,
    CustomerName VARCHAR2(50),
    AccountType VARCHAR2(20),
    Balance NUMBER(10,2)
);

CREATE TABLE Employees (
    EmployeeID NUMBER PRIMARY KEY,
    EmployeeName VARCHAR2(50),
    Department VARCHAR2(30),
    Salary NUMBER(10,2)
);

INSERT INTO Accounts VALUES (201, 'Aarav', 'Savings', 15000);
INSERT INTO Accounts VALUES (202, 'Sneha', 'Savings', 25000);
INSERT INTO Accounts VALUES (203, 'Vikram', 'Current', 18000);
INSERT INTO Accounts VALUES (204, 'Neha', 'Savings', 35000);

INSERT INTO Employees VALUES (101, 'Rahul', 'Finance', 55000);
INSERT INTO Employees VALUES (102, 'Pooja', 'Finance', 65000);
INSERT INTO Employees VALUES (103, 'Karan', 'Marketing', 48000);
INSERT INTO Employees VALUES (104, 'Aisha', 'Marketing', 58000);

COMMIT;

Select * from Accounts;
SELECT * FROM Employees;

--Scenario 1

CREATE OR REPLACE PROCEDURE ProcessMonthlyInterest
IS
BEGIN
   UPDATE Accounts
   SET Balance = Balance + (Balance * 0.01)
   WHERE AccountType = 'Savings';

   COMMIT;
END;
/

BEGIN
   ProcessMonthlyInterest;
END;
/

Select * from Accounts;

--Scenario 2

CREATE OR REPLACE PROCEDURE UpdateEmployeeBonus(
   p_department IN VARCHAR2,
   p_bonus_percent IN NUMBER
)
IS
BEGIN
   UPDATE Employees
   SET Salary = Salary + (Salary * p_bonus_percent / 100)
   WHERE Department = p_department;

   COMMIT;
END;
/


BEGIN
   UpdateEmployeeBonus('IT',10);
END;
/

SELECT * FROM Employees;

--Scenario 3

CREATE OR REPLACE PROCEDURE TransferFunds(
   p_from_account IN NUMBER,
   p_to_account IN NUMBER,
   p_amount IN NUMBER
)
IS
   v_balance NUMBER;
BEGIN
   SELECT Balance
   INTO v_balance
   FROM Accounts
   WHERE AccountID = p_from_account;

   IF v_balance >= p_amount THEN

      UPDATE Accounts
      SET Balance = Balance - p_amount
      WHERE AccountID = p_from_account;

      UPDATE Accounts
      SET Balance = Balance + p_amount
      WHERE AccountID = p_to_account;

      COMMIT;

      DBMS_OUTPUT.PUT_LINE('Transfer Successful');

   ELSE
      DBMS_OUTPUT.PUT_LINE('Insufficient Balance');
   END IF;
END;
/

SET SERVEROUTPUT ON;

BEGIN
   TransferFunds(101,102,5000);
END;
/

Select * from Accounts;